import 'package:flutter/material.dart';

class AccountScreenAdmin extends StatelessWidget {
  const AccountScreenAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: const SafeArea(
          child: Scaffold(
        backgroundColor: Colors.white,
      )),
    );
  }
}
