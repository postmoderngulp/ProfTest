import 'package:flutter/material.dart';

class AccountScreenUser extends StatelessWidget {
  const AccountScreenUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: const SafeArea(
          child: Scaffold(
        backgroundColor: Colors.white,
      )),
    );
  }
}
